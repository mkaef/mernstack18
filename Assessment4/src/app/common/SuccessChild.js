import React from "react"

 let SuccessChild =({props}) => {

   return(

        this.state = {
            name : props.parentName,
            refAddress : "Odessa Texas",
            
        }
    )
}

export default SuccessChild;